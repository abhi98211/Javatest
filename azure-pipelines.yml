trigger:
- main

variables:
 test: 'Raj'

pool:
 vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build
  jobs:
    - job: new
      steps:
      - script: |
         echo "Abhi-$(test)"
- stage: Test
  displayName: test
  dependsOn: Build
  isSkippable: false
  jobs:
    - job: tee
      steps:
      - script: |
          echo "Test"
    - job: job 
      steps:
      - script: |
          echo "testb"
  
